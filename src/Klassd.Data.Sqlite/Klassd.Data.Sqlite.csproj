<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>Klassd.Data.Sqlite</PackageId>
    <Description>SQLite storage adapter for Klassd. Raw Microsoft.Data.Sqlite + JSON in TEXT (no EF/ORM), single-database.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.Sqlite" />
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <!-- Attaches the matching Klassd.Auth SQLite store to the same database. -->
    <PackageReference Include="Klassd.Auth.Abstractions" />
    <PackageReference Include="Klassd.Auth.Data.Sqlite" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Klassd.Abstractions\Klassd.Abstractions.csproj" />
  </ItemGroup>

</Project>
